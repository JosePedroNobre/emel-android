package com.emel.app.utils

import android.Manifest

object AppConstants {

    const val CONNECT_TIMEOUT: Long = 60 * 1000
    const val READ_TIMEOUT: Long = 60 * 1000
    const val WRITE_TIMEOUT: Long = 60 * 1000
    const val PIONEER_PREFERENCES = "PIONEER_PREFERENCES"
    const val PREF_TOKEN = "PREF_TOKEN"
    const val PREF_REFRESH_TOKEN = "PREF_REFRESH_TOKEN"

}